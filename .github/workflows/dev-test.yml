name: "Netree Frontend CI Pipeline"
run-name: "run name: netree frontend being deployed by ${{github.actor}}"
on:
  push:
    branches:
      - main
      - 'dev/**'
jobs:
  lint-client:
    runs-on: ubuntu-latest
    steps:
      - run: echo "initiating deployment for client"
      - name: Get code
        uses: actions/checkout@v4
      - run: echo "repo downloaded"
      - name: Run script
        run: |
          echo "Changing to the client directory"
          cd client
          pwd
          ls
          echo "Installing dependencies"
          yarn install
          echo "Linting"
          yarn run lint
          echo "linting done| now building app"
          yarn build
  build-clint-artifact:
    needs: client-pipeline
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "now building package to artifact"
          cd client
      - name: Upload a build Artifact
      uses: actions/upload-artifact@v4.1.0
      with:
        - name: netree-client-artifact
        - path : client/dist
  deploy-client-artifact:
    needs: build-client-artifact
    runs-on: ubuntu-latest
    steps:
      - name: download artifact
        uses: actions/download-artifact@v4.1.1
        with:
          name: netree-client-artifact
      - name: output content of artifact
        run: ls
      - name: Deploy client
        run: "deploying the client to ...."




            